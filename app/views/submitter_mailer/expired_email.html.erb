<p><%= t('hi_there') %>,</p>
<p><%= t('submission_name_has_expired', name: @submission.name || @submission.template.name) %></p>
<p><%= link_to submission_url(@submission), submission_url(@submission) %></p>
